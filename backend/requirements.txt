# MARS-PaperPulse Backend Dependencies
# -------------------------------------
# Version pins use compatible-release specifiers to balance reproducibility
# with security patches. Run `pip install -r requirements.txt` to install.

# Core framework
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
pydantic>=2.5.0,<3.0.0
pydantic-settings>=2.5.0,<3.0.0
structlog>=24.0.0
python-dotenv>=1.0.0
python-multipart>=0.0.9

# HTTP clients
aiohttp>=3.9.0
httpx>=0.27.0
requests>=2.32.0

# PDF handling
pymupdf>=1.24.0
pypdf>=4.0.0

# Stage 5 enhanced PDF report generation
reportlab>=4.0.0        # programmatic A4 magazine-style PDF
Pillow>=10.0.0          # gradient/image processing for cover & about pages
numpy>=1.24.0           # gradient arrays (also required by matplotlib above)

# Agent graph (paper_agents / langgraph_agents)
langgraph>=0.2.0
langchain-google-genai>=2.0.0
langchain-openai>=0.1.0
langchain-anthropic>=0.1.0
# AWS Bedrock support for Stage 4 paper generation
langchain-aws>=0.2.0

# Credential encryption
cryptography>=42.0.0

# CMB Agent (LLM provider registry + orchestration)
# ─────────────────────────────────────────────────────────────────────────────
# PaperPulse needs the [deepresearch] composite extra so the engineer agent
# can execute Python locally (statsmodels, matplotlib, scipy, sklearn, ...).
# For local development (editable install from checkout):
# -e /home/sachin_mourya/Desktop/mars_cmbagent[all]
# For PyPI release (uncomment when published, comment the -e line above):
mars_cmbagent[all]>=2.2.3

# Scientific stack safety-net: these come from mars_cmbagent[local,data] but
# are pinned here too so a forgetful install order still gives the engineer
# what it needs to actually run experiments instead of writing prose.
statsmodels>=0.14.0
matplotlib>=3.8.0
scipy>=1.12.0
scikit-learn>=1.4.0

# Authentication
argon2-cffi>=23.1.0               # memory-hard password hashing (Argon2 backend)
passlib[argon2]>=1.7.4            # password hashing context (wraps argon2-cffi)
python-jose[cryptography]>=3.3.0  # JWT signing/verification
slowapi>=0.1.9                     # rate limiting (brute-force protection)
email-validator>=2.0.0             # required by Pydantic EmailStr in auth router

# Tracing / Observability (already pulled by mars_cmbagent[tracing]; pinned here for explicitness)
opentelemetry-sdk>=1.44.0
opentelemetry-exporter-otlp-proto-http>=1.44.0
openinference-instrumentation-langchain>=0.1.67   # traces stage 4-5 LangGraph/LangChain LLM calls into Langfuse

# Test dependencies
pytest>=8.0.0
pytest-asyncio>=0.23.0
sqlalchemy
langchain
langchain_community