# MARS-PaperPulse Backend Dependencies
# -------------------------------------
# Version pins use compatible-release specifiers to balance reproducibility
# with security patches. Run `pip install -r requirements.txt` to install.

# Core framework
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
pydantic>=2.5.0,<3.0.0
pydantic-settings>=2.5.0,<3.0.0
structlog>=24.0.0
python-dotenv>=1.0.0

# HTTP clients
aiohttp>=3.9.0
httpx>=0.27.0
requests>=2.32.0

# PDF handling
pymupdf>=1.24.0
pypdf>=4.0.0

# Credential encryption
cryptography>=42.0.0

# CMB Agent (LLM provider registry + orchestration)
mars_cmbagent>=1.1.2

# LangChain adapters for direct LLM calls inside PaperPulse agents
# (cmbagent already pulls in langchain-openai / -anthropic / -google-genai
#  and litellm; langchain-aws is the Bedrock chat model for LangGraph nodes.)
langchain-aws>=0.2.0

# Test dependencies
pytest>=8.0.0
pytest-asyncio>=0.23.0
